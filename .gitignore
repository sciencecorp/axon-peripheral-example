build/
build-*/
dist/*.deb
sdk/*.deb
.vscode/
.cache/
compile_commands.json
.DS_Store
!dist/.gitkeep
!sdk/.gitkeep
.venv
# synapsectl stores SFTP credentials here on `--save-password`. Never commit.
.scienv

# Recordings captured during local testing.
*.h5
test.h5/

# Superpowers brainstorming/spec scratch — local only.
docs/superpowers/
